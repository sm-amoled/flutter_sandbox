import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class TextSenderPage extends StatelessWidget {
  const TextSenderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("Text Sender")),
        body: Container(
          child: Center(
              // child: FormField(
              //   builder: (field) {
              //     return TextField();
              //   },
              // ),
              
              ),
        ));
  }
}
